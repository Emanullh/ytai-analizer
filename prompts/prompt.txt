Tarea: Implementar Description Features Agent (determinista + LLM via AutoGen) integrado al flujo actual de export (sin pasos extra en frontend)

Contexto:
- Ya existe AutoGen runtime del primer agente:
  - apps/api/scripts/autogen_worker.py
  - apps/api/src/services/autogenRuntime.ts
  - env vars AUTO_GEN_ENABLED, AUTO_GEN_MODEL_*, etc.
- Export flow asíncrono con SSE:
  - exportJobService -> exportService
- Artefactos derivados:
  - exports/<channel>/derived/video_features/<videoId>.json (ya creado por Title agent)

Objetivo:
Agregar descripción (description) como fuente de features derivadas por video y persistir en el MISMO template de salida reusable.

Salida (template fijo, extendiendo sin romper lo existente):
exports/<channel>/derived/video_features/<videoId>.json debe incluir además:
{
  "schemaVersion": "derived.video_features.v1",
  "videoId": "...",
  "computedAt": "ISO",
  "titleFeatures": { ... },            // existente
  "descriptionFeatures": {
    "deterministic": { ... },
    "llm": { ... },
    "warnings": [...]
  }
}
Regla: si ya existe el JSON por title agent, debe “mergearse” (leer->merge->write) sin perder campos.

IMPORTANTE: Qué se calcula SIN AutoGen (determinista) y NO debe ir al prompt LLM
Implementar en TypeScript (Node) y NO llamar LLM para:

A) Estructura:
- desc_len_chars, desc_len_words
- line_count
- has_timestamps (regex: (\d{1,2}:)?\d{1,2}:\d{2})
- url_count + urls[] extraídas (normalizadas)
- domain_counts (top dominios)
- hashtag_count (#\w+)
- mentions_count (@\w+)
- cta_count por reglas (diccionario ES/EN):
  - subscribe/suscríbete/suscribete, like/me gusta, comment/comenta, link/enlace, follow/sígueme, newsletter, patreon, etc.
- cta_in_first_200_chars (bool)

B) SEO / overlap:
- title_desc_overlap_jaccard sobre tokens normalizados (min length >=2)
- title_desc_overlap_tokens: {titleTokens:[], hitTokens:[]} como evidencia (audit)

C) Flags de disclosure (reglas, no LLM):
- has_sponsor_disclosure:
  - patrones: "sponsor", "sponsored", "patrocinado", "patrocinio", "ad", "paid promotion"
- has_affiliate_disclosure:
  - patrones: "affiliate", "afiliado", "affiliate link", "enlace afiliado"
- has_credits_sources:
  - patrones: "sources", "fuentes", "references", "créditos", "credit", "further reading" + url_count>0
Guardar evidence: matches con (charStart,charEnd,snippet).

D) Readability (determinista):
- readability:
  - si texto parece ES: Fernández-Huerta o Szigriszt-Pazos (implementación aproximada aceptable)
  - si EN: Flesch Reading Ease
- Guardar {metric, score} y warning si language desconocido.

E) Link list determinista:
- urls[] con:
  - url, domain, charStart, charEnd (span exacto en description)
  - isShortener (bit.ly, t.co, youtu.be, etc.)
No clasificar propósito con reglas (eso va al LLM).

Qué SÍ se calcula CON AutoGen (LLM) y debe ir al prompt del agente:
- linkPurpose classification (cerrado) para cada URL detectada (o top N=10):
  sponsor | affiliate | sources | social | merch | newsletter | community | other
  Con:
  - confidence 0..1
  - evidence: referencia al span del URL (charStart/charEnd/snippet)
- sponsorBrandMentions (si existen): lista de marcas mencionadas (closed? no; pero debe venir como strings) + evidence spans en description
- primaryCTA (cerrado): subscribe|like|comment|link|follow|none + confidence + evidence

Implementación requerida (apps/api):
1) Crear módulo determinista:
   - apps/api/src/derived/descriptionDeterministic.ts
     export function computeDescriptionDeterministicFeatures({title, description}): {features, evidence, warnings}
   Incluye helpers: tokenize, extractUrlsWithSpans, domainCounts, regex flags, readability.

2) Crear agente:
   - apps/api/src/derived/descriptionFeaturesAgent.ts
     - computeDeterministic(...)
     - if AUTO_GEN_ENABLED && OPENAI_API_KEY: call autogenRuntime.request({task:"description_classifier_v1", payload:{videoId, title, description, urlsWithSpans, languageHint}})
     - merge deterministic + llm
     - return bundle + warnings

3) Extender autogen_worker.py para soportar el nuevo task:
   - task == "description_classifier_v1"
   - usar SYSTEM PROMPT fijo (ver “PROMPT AUTOGEN — DescriptionClassifierAgent v1” al final)
   - validar JSON parseable (no markdown)
   Nota: NO re-hagas bootstrap, solo agrega handler.

4) Integración export:
   - En apps/api/src/services/exportService.ts
     - tras obtener video details (title/description) y transcript (ya existe)
     - invocar descriptionFeaturesAgent y persistir merged JSON en derived/video_features/<videoId>.json
   - SSE:
     - NO agregar nuevo stage si no es necesario; reusa "writing_json" con percent incremental.
     - Si agregas stage, actualiza apps/web/src/types.ts + reducer + tests.

5) Persistencia:
   - Asegurar carpeta exports/<channel>/derived/video_features/
   - ensureInsideRoot para derived.
   - Escritura atómica: write temp + rename.

6) Tests:
   - apps/api/tests/descriptionDeterministic.test.ts:
     - timestamps, urls, domain_counts, hashtags, mentions
     - disclosure flags + evidence spans
     - title_desc_overlap_jaccard
   - apps/api/tests/descriptionFeaturesAgent.test.ts:
     - con AutoGen deshabilitado: llm=null + warnings, deterministic ok
     - con autogenRuntime mock: verifica merge de llm
   - Export integration test:
     - derived/video_features/<videoId>.json contiene descriptionFeatures

7) Docs:
   - docs/DATA_CONTRACTS.md: agregar descriptionFeatures al derived schema
   - AI Context Pack: mention new task "description_classifier_v1"

PROMPT AUTOGEN — DescriptionClassifierAgent v1:
(usa EXACTAMENTE el bloque del siguiente apartado; no inventes otro)

Entrega:
- pnpm test verde
- export job genera derived con descriptionFeatures para cada video
- sin pasos nuevos en frontend